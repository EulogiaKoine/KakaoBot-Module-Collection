/**
 * @name BlockExecutor
 * @author E. Koinē
 * @version 1.0.0(2024-01-16)
 * @description
 * 
 * 시나리오 내 블럭 연산 의존성 분리.
 * 플레이어 데이터를 참조한다.
 * 타입별 블럭을 다르게 연산한다.
 * 
 */

"use strict"
module.exports = function(_, $rspire){
const { Scenario } = $rspire('Scenario')
const BlockType = [
    "text", // 단순 문자열
    "single-command", // 단일 함수(커맨드)
    "codes", // 
    ""
]




}